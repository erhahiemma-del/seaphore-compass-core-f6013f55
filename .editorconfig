root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
indent_style = space
indent_size = 2
trim_trailing_whitespace = true

# Trailing whitespace is significant in Markdown (two spaces = hard line break).
[*.md]
trim_trailing_whitespace = false
