# stores/

Zustand stores for **global client state**. One store per concern.

Business logic is intentionally **not** implemented yet — these files
scaffold the shape defined in the Build Bible so features can hook in
without ad-hoc React context. Populate actions as features need them.
