/**
 * Global immutable footer — Evidence first. Explainable always. Officer decides.
 * Rendered by IntelligenceCentreShell on every screen. Text is fixed and
 * MUST NOT be overridden.
 */
export function Footer() {
  return (
    <footer className="border-t border-line bg-surface-1 px-4 py-2 text-center text-[11px] tracking-wide text-slate">
      Evidence first. Explainable always. Officer decides.
    </footer>
  );
}
