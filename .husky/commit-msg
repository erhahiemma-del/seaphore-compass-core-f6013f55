# Guard against WIP/fixup commits sneaking into main.
commit_msg=$(cat "$1")
case "$commit_msg" in
  "fixup!"*|"squash!"*|"WIP"*|"wip"*)
    echo "✖ Commit message starts with a WIP/fixup prefix. Amend before landing." >&2
    exit 1
    ;;
esac
